import React, { forwardRef } from 'react'

const Projects = forwardRef((props, ref) => {
    return (
        <section id="projects" ref={ref} className='pt-16 md:pt-16'>

            <h1>Projects</h1>
        </section>
    )
})

export default Projects